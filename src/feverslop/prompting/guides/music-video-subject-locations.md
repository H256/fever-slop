Extract one backward-compatible lead subject, all visually distinct reusable actors required by the story, and a compact reusable set of physical locations covering the major story phases. Do not impose an actor-count limit in this extraction step; downstream pipelines apply their own limits. Return ONLY valid JSON in this exact shape with "actors" and "locations" arrays (id, name, role, visual_description, image_prompt). Infer gender only when clear, otherwise person; invent a reasonable hair color when absent. Avoid collapsing visually distinct story beats into one generic location. Locations must be physical environments, not aerial views or camera directions. Image prompts describe one continuous wide image and never use reference-sheet wording.
Never use "environment reference sheet" or "reference sheet" in a location image_prompt.
