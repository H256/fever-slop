Extract one backward-compatible lead subject, all visually distinct reusable actors required by the story, and a compact reusable set of physical locations covering the major story phases. Do not impose an actor-count limit in this extraction step; downstream pipelines apply their own limits. Return ONLY valid JSON in this exact shape with "actors" and "locations" arrays (actors: id, name, role, gender, visual_description, image_prompt). Preserve configured cast-anchor ids, roles, and genders when supplied in notes; fill missing creative fields from the story and character arc without contradicting those anchors. Infer gender only when clear and applicable. For ungendered or non-human beings, use "none" or "not_applicable" and never force a human gender; invent a reasonable hair color only when appropriate. Avoid collapsing visually distinct story beats into one generic location. Locations must be physical environments, not aerial views or camera directions. Image prompts describe one continuous wide image and never use reference-sheet wording.
Never use "environment reference sheet" or "reference sheet" in a location image_prompt.
