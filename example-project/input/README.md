# Benchmark audio input

Place the fixed one-minute benchmark track here as `song.mp3`.

Do not add temporary exports, alternate mixes, stems, or generated renders to
this directory. Complete `../asset-provenance.json` before committing the MP3.
