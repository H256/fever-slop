# Benchmark audio input

The fixed benchmark track is `the-parts-they-left.mp3`.

Do not add temporary exports, alternate mixes, stems, or generated renders to
this directory. If the canonical MP3 changes, update and review
`../asset-provenance.json` in the same commit.
